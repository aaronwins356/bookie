from __future__ import annotations

# Sport-specific modeling layer. Each sub-package is self-contained and
# integrates with the shared engine, router, risk, and backtest systems
# through the replay_adapter pattern.

SUPPORTED_SPORTS = ["tennis"]
